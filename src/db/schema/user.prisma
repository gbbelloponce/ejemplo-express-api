model User {
  id        String @id @default(cuid())
  firstName String
  lastName  String
  email     String @unique
  password  String

  posts Post[]

  createdAt DateTime  @default(now())
  deletedAt DateTime?

  @@map("users")
}
